class AppGlobals {
  const AppGlobals();
  static const String appName = 'Flutter Hooks App';
  static const Duration iconAnimationDuration = Duration(milliseconds: 450);
  static const Duration animationDuration = Duration(milliseconds: 750);
  static const Duration rotationDuration = Duration(milliseconds: 2500);
}
